// Talks to Google Gemini AI.
// Sends: the full conversation history + Sage's system prompt
// Gets back: what Sage should say next (plain text)
// Rule: Gemini has NO memory — we send the full history every time.
