// Sends SMS / iMessage alerts to emergency contacts.
// Uses Photon's API (hackathon sponsor).
// sendAlertToAll(contacts, userName) → texts everyone at once.
// buildCheckInMessage(userName) → softer daily check-in text.
