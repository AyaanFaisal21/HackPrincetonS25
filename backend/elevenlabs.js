// Converts Sage's text response into audio (mp3).
// Sends: the text Sage is about to say
// Gets back: an mp3 file URL the frontend can play
// Saves mp3 files to backend/audio_cache/ folder temporarily.
